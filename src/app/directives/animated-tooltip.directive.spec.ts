import { AnimatedTooltipDirective } from './animated-tooltip.directive';

describe('AnimatedTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
